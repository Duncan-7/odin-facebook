<%= form_for(current_user.friendships.find_by(user_id: current_user.id, friend_id: @user.id), html: { method: :delete }) do |f| %>
  <div><%= hidden_field_tag :friend_id, @user.id %></div>
  <%= f.submit "Remove Friend", class: "btn btn-primary" %>
<% end %>