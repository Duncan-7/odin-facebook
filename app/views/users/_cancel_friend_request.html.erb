<%= form_for(current_user.sent_requests.find_by(receiver_id: @user.id), html: { method: :delete }) do |f| %>
  <div><%= hidden_field_tag :receiver_id, @user.id %></div>
  <%= f.submit "Cancel Friend Request", class: "btn btn-primary" %>
<% end %>